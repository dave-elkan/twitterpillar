# eDave's shared templates demo
An example app showing how to pre-compile templates, share them with the browser and use the History API to maintain URL state. 

# How to run
	$ npm install -d
	$ node app.js
